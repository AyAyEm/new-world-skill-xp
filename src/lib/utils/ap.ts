// Arithmetic progression https://en.wikipedia.org/wiki/Arithmetic_progression
export const ap = (start: number, n: number, difference: number): number => start + (n - 1) * difference;

export default ap;
